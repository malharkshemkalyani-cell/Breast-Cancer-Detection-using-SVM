# Breast-Cancer-Detection-using-SVM
This program performs a classification task using a Support Vector Machine (SVM) on a dataset related to cell samples. It includes steps for data loading, preprocessing (handling missing values and type conversion), splitting data into training and testing sets, training an SVM model.
